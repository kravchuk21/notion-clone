# TaskFlow - Trello-like App с дизайном Notion

Полнофункциональное веб-приложение для управления задачами в стиле Trello с современным минималистичным дизайном в духе Notion.

![TaskFlow Preview](https://via.placeholder.com/800x400?text=TaskFlow+Preview)

## ✨ Возможности

### Аутентификация
- 🔐 Регистрация и вход с email/паролем
- 🍪 Безопасные httpOnly cookies для JWT
- 🛡️ Защищённые маршруты

### Доски и колонки
- 📋 Создание/редактирование/удаление досок
- 📊 Неограниченное количество колонок
- 🔄 Drag & Drop для перемещения колонок
- ✏️ Редактирование названий инлайн

### Карточки задач
- ➕ Создание карточек в любой колонке
- 📝 Название, описание, приоритет, метки, дедлайн
- 🎯 Три уровня приоритета с цветовой индикацией
- 🏷️ Метки/теги
- 📅 Дедлайны с индикацией просроченных задач
- 🔄 Drag & Drop между колонками

### Фильтрация и поиск
- 🔍 Поиск по названию и описанию
- 🎚️ Фильтр по приоритетам
- 🏷️ Фильтр по меткам
- ⏰ Фильтр просроченных задач
- 🔄 Сброс всех фильтров

### Real-time обновления
- ⚡ WebSocket подключение через Socket.io
- 🔄 Автоматическое обновление при изменениях на других устройствах

### Тёмная тема
- 🌙 Светлая/тёмная/системная темы
- 💾 Сохранение предпочтений

## 🛠 Технологический стек

### Frontend
- **React 18** + TypeScript
- **Vite** - сборка
- **Tailwind CSS** - стилизация
- **@dnd-kit** - Drag & Drop
- **TanStack Query** - серверный стейт
- **Zustand** - клиентский стейт
- **Socket.io-client** - real-time

### Backend
- **Node.js** + Express + TypeScript
- **Prisma** - ORM
- **PostgreSQL** - база данных
- **Socket.io** - WebSocket сервер
- **JWT** + httpOnly cookies - аутентификация
- **Zod** - валидация

### DevOps
- **Docker** + Docker Compose
- **Nginx** - раздача статики в production

## 🚀 Быстрый старт

### Требования
- Docker 24+
- Docker Compose 2+
- Make (опционально)

### Запуск

1. **Клонируйте репозиторий**
```bash
git clone <repo-url>
cd notion
```

2. **Создайте файл окружения**
```bash
cp env.example .env
```

3. **Запустите приложение**
```bash
# Через Make
make dev

# Или через Docker Compose
docker-compose up --build
```

4. **Откройте в браузере**
```
http://localhost:3000
```

## 📁 Структура проекта

```
notion/
├── frontend/               # React приложение
│   ├── src/
│   │   ├── api/           # API клиент
│   │   ├── components/    # React компоненты
│   │   ├── hooks/         # Custom hooks
│   │   ├── lib/           # Утилиты (socket)
│   │   ├── pages/         # Страницы
│   │   ├── store/         # Zustand stores
│   │   ├── types/         # TypeScript типы
│   │   └── utils/         # Хелперы
│   ├── Dockerfile
│   └── nginx.conf
│
├── backend/               # Express сервер
│   ├── src/
│   │   ├── config/       # Конфигурация
│   │   ├── controllers/  # Обработчики запросов
│   │   ├── middleware/   # Middleware
│   │   ├── routes/       # Маршруты
│   │   ├── schemas/      # Zod схемы
│   │   ├── services/     # Бизнес-логика
│   │   └── utils/        # Утилиты
│   └── Dockerfile
│
├── prisma/               # Prisma ORM
│   └── schema.prisma    # Схема БД
│
├── docker-compose.yml   # Development
├── docker-compose.prod.yml  # Production override
├── Makefile            # Команды управления
└── env.example         # Пример переменных
```

## 🔧 Переменные окружения

| Переменная | Описание | По умолчанию |
|------------|----------|--------------|
| `POSTGRES_USER` | Пользователь PostgreSQL | `notion_user` |
| `POSTGRES_PASSWORD` | Пароль PostgreSQL | `notion_secure_password_123` |
| `POSTGRES_DB` | Название базы данных | `notion_trello` |
| `JWT_SECRET` | Секрет для JWT (мин. 32 символа) | - |
| `PORT` | Порт бэкенда | `5000` |
| `NODE_ENV` | Окружение | `development` |
| `FRONTEND_URL` | URL фронтенда для CORS | `http://localhost:3000` |
| `VITE_API_URL` | URL API для фронтенда | `http://localhost:5000/api` |

## 📋 API Документация

### Auth
| Метод | Эндпоинт | Описание |
|-------|----------|----------|
| POST | `/api/auth/register` | Регистрация |
| POST | `/api/auth/login` | Вход |
| POST | `/api/auth/logout` | Выход |
| GET | `/api/auth/me` | Текущий пользователь |

### Boards
| Метод | Эндпоинт | Описание |
|-------|----------|----------|
| GET | `/api/boards` | Список досок |
| GET | `/api/boards/:id` | Доска с данными |
| POST | `/api/boards` | Создать доску |
| PUT | `/api/boards/:id` | Обновить доску |
| DELETE | `/api/boards/:id` | Удалить доску |

### Columns
| Метод | Эндпоинт | Описание |
|-------|----------|----------|
| POST | `/api/boards/:boardId/columns` | Создать колонку |
| PUT | `/api/columns/:id` | Обновить колонку |
| DELETE | `/api/columns/:id` | Удалить колонку |
| PATCH | `/api/columns/reorder` | Переупорядочить |

### Cards
| Метод | Эндпоинт | Описание |
|-------|----------|----------|
| POST | `/api/columns/:columnId/cards` | Создать карточку |
| PUT | `/api/cards/:id` | Обновить карточку |
| DELETE | `/api/cards/:id` | Удалить карточку |
| PATCH | `/api/cards/:id/move` | Переместить |
| PATCH | `/api/cards/reorder` | Переупорядочить |

## 🛠 Команды Make

```bash
make dev          # Запуск в dev режиме
make dev-d        # Запуск в фоне
make prod         # Запуск в production
make down         # Остановить контейнеры
make clean        # Очистить всё
make logs         # Просмотр логов
make logs-backend # Логи бэкенда
make restart      # Перезапуск
make db-reset     # Сброс БД
make db-migrate   # Миграции
make db-studio    # Prisma Studio
make shell-backend # Shell в бэкенд
make shell-db     # Shell в PostgreSQL
```

## 🐛 Troubleshooting

### Порты заняты
```bash
# Проверить занятые порты
lsof -i :3000
lsof -i :5000
lsof -i :5432

# Убить процесс
kill -9 <PID>
```

### Проблемы с БД
```bash
# Сбросить БД
make db-reset

# Перегенерировать Prisma клиент
docker-compose exec backend npx prisma generate
```

### Очистка Docker
```bash
# Полная очистка
make clean

# Или вручную
docker-compose down -v --rmi local
docker system prune -af
```

## 📄 Лицензия

MIT

---

Создано с ❤️ используя React, Node.js и Docker

